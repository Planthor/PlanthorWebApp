version: 2.1
orbs:
    node: circleci/node@4.3.0
jobs:
    build:
        executor:
            name: node/default
            tag: "13.14"
        steps:
            - checkout
            - node/install-packages
            - node/run:
                npm-run: test 
                pkg-manager: npm 
                resource_class: large 


workflows:
    planthor-fe-ci:
        jobs:
            - build