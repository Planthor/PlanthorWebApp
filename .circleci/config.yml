version: 2.1
orbs:
    node: circleci/node@4.3.0
jobs:
    build:
        executor:
            name: node/default
            tag: "13.14"
        steps:
            - checkout
            - node/install-packages
            - run:
                name: test & build
                command: npm run check && npm build


workflows:
    planthor-fe-ci:
        jobs:
            - build