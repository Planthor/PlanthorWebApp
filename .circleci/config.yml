version: 2.1
orbs:
    node: circleci/node@5.1.0

workflows:
    planthor-fe-ci:
        jobs:
        - node/run:
            npm-run: test 
            pkg-manager: npm 
            resource_class: large 