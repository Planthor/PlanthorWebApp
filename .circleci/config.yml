version: 2.1
orbs:
  node: circleci/node@5.1.0

workflows:
    example-workflow:
      jobs:
        - node/run:
            app-dir: ./ 
            cache-version: v1 
            npm-run: test 
            pkg-manager: npm 
            resource_class: large 